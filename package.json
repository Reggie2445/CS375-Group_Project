{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.6.4",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^13.5.0",
    "antd": "^5.26.7",
    "axios": "^1.11.0",
    "cookie-session": "^2.1.1",
    "cors": "^2.8.5",
    "firebase": "^12.0.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-firebase-hooks": "^5.1.1",
    "react-router-dom": "^7.7.1",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4",
    "concurrently": "^8.2.2",
    "serve": "^14.2.1"
  },

  "comments": {
    "start_note": "Remember to update version before release",
    "windows_start": "react-scripts start",
    "linux_start": "HOST=127.0.0.1 react-scripts start"
  },

  "scripts": {
    "start:client": "react-scripts start",
    "start:server": "node src/server.js",
    "start:prod": "serve -s build -l $PORT",
    "test:prod": "npm run build && concurrently \"npm run start:server\" \"serve -s build -l 3000\"",
    "server": "node src/server.js",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
